# Minimos, no versiones exactas, a proposito.
#
# Los pines exactos (numpy==2.1.1, scipy==1.14.1, pydantic==2.9.2...) son de
# paquetes anteriores a Python 3.14: no publican wheels cp314, asi que en un
# servidor con 3.14 pip intentaria compilarlos desde fuente y haria falta
# toolchain de C, C++, Fortran y Rust. Con minimos, pip resuelve la version
# que si tiene wheel para el interprete que haya.
#
# Probado con Python 3.11 + fastapi 0.115.0, pydantic 2.9.2, numpy 2.1.1,
# scipy 1.14.1. El codigo solo usa API estable de cada uno.
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.2
pydantic-settings>=2.5.2
aiohttp>=3.10.5
websockets>=13.1
numpy>=2.1.1
python-dotenv>=1.0.1

# scipy es OPCIONAL: solo acelera la EMA (src/utils/fastmath.py). Si no se
# puede instalar, el sistema cae al bucle Python y funciona igual, mas lento.
scipy>=1.14.1
